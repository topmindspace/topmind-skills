# topmind Doctor

Use when the user asks to check topmind health, UTR status, MCP exposure, skill drift, or cleanup candidates.

Preferred command:

```bash
node utr/bin/topmind-cli.mjs doctor --json --mcp
```

Report evidence and recommended follow-up. Apply cleanup only according to `writebackMode`.
